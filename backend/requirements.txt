# IBridge HR Portal — Backend Dependencies

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pydantic[email]>=2.7.0

# Required for file upload endpoints (branding logo/signature) — FastAPI's
# UploadFile / File(...) params raise at import time without this installed.
python-multipart>=0.0.9

# Supabase (database + auth + storage)
supabase>=2.5.0

# JWT validation (verified against Supabase's JWKS endpoint, which can
# serve HS256, RS256, or ES256 keys depending on project configuration —
# the crypto extra is required for the latter two, not just HS256)
PyJWT[crypto]>=2.8.0

# Email
resend>=2.3.0

# Document generation (from existing desktop app, unchanged)
python-docx>=1.1.2
num2words>=0.5.13
openpyxl>=3.1.5

# PDF conversion (LibreOffice headless, triggered via subprocess)
# No Python package needed — LibreOffice is installed on the server

# Scheduled jobs (DOJ reminders, relieving reminders)
apscheduler>=3.10.4
